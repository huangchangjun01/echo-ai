# Weaviate (REST only — we talk to the v1 REST API directly, so gRPC is not used)
WEAVIATE_URL=
WEAVIATE_HOST=localhost
WEAVIATE_SCHEME=http
WEAVIATE_PORT=8080
WEAVIATE_CLASS=EchoDoc
WEAVIATE_API_KEY=

# Qiniu
QINIU_BASE_URL=
# Optional: comma-separated list of allowed Qiniu subdomains for SSRF guard.
# When set, downloads whose resolved hostname does not end with one of these are rejected.
QINIU_ALLOWED_SUBDOMAINS=

# Embedding
EMBEDDING_MODEL_NAME=OFA-Sys/chinese-clip-vit-base-patch16
EMBEDDING_DEVICE=auto
EMBEDDING_DIM=512
EMBEDDING_BATCH_SIZE=8
EMBEDDING_CHUNK_SIZE=256
EMBEDDING_CHUNK_OVERLAP=32
EMBEDDING_WARMUP_ON_START=true

# Ingest
INGEST_MAX_DOWNLOAD_BYTES=52428800
INGEST_DOWNLOAD_TIMEOUT_SECONDS=60
INGEST_DOWNLOAD_RETRIES=3
INGEST_ENABLE_CHUNKING=true
INGEST_CACHE_TTL_SECONDS=60
INGEST_CACHE_MAXSIZE=256

# Retrieval
VECTOR_SIMILARITY_THRESHOLD=0.7