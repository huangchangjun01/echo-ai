fastapi==0.104.1
uvicorn[standard]==0.24.0
torch==2.6.0
transformers==4.35.0
Pillow==10.1.0
python-multipart==0.0.6

# LangChain + Vector DB
langchain>=0.0.300
langchain-core>=0.1.0
langchain-text-splitters>=0.0.1
sentence-transformers>=2.2.2

# Weaviate v4 client
weaviate-client>=4.5.0

# Config / validation
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Async HTTP + retry + caching
httpx>=0.25.0
tenacity>=8.2.0
cachetools>=5.3.0

# File type detection
python-magic-bin>=0.4.14; sys_platform == "win32"
python-magic>=0.4.27; sys_platform != "win32"

# Object storage SDK (echo-ai 直传/删除对象，七牛)
qiniu>=7.13.0

# Tests
pytest>=7.4.0
pytest-asyncio>=0.23.0

# ---------- 音频转录（可选，缺失时音频会走"转录失败"占位兜底）----------
# openai-whisper 用于本地转录音频；首次运行会自动下载模型权重。
#   默认档位 medium（约 1.5GB）——base 在中文上同音字错误严重（实测把"上吐下泻"
#   识别成"上土下泄"、"一整晚"识别成"一整碗"），不可用于中文。
#   降档只需环境变量 WHISPER_MODEL=small，无需改代码。
# imageio-ffmpeg 内置 ffmpeg 可执行文件，让 Windows 上无需单独装 ffmpeg。
#   注意它装出来的文件叫 ffmpeg-win-x86_64-*.exe 而非 ffmpeg.exe，
#   所以代码里按绝对路径调用，不依赖 PATH 的名字解析。
# opencc 做繁转简：whisper 中文倾向输出繁体（small 以上尤其明显）。
#   缺失时降级为不转换，不影响转录本身。
# 安装方式：pip install openai-whisper imageio-ffmpeg opencc-python-reimplemented
# openai-whisper>=20231117
# imageio-ffmpeg>=0.5.0
# opencc-python-reimplemented>=0.1.7